const jsonFetcher = () => {
  let data = $('#JSONtext').val();
  console.log(data);

  // fetch('/JSONData', {
  //   method: 'POST',
  //   mode: 'same-origin',
  //   cache: 'no-cache'
  //   credentials:

  // })
}